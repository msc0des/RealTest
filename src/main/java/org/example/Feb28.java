package org.example;

public class Feb28 {
    //Amadeus labs
    //sort numbers greater than 15
       /* List<Integer> list=Arrays.asList(11,12,3,4,55,77,128);
        List<Integer>ans=list.stream().filter(x->x>15).sorted(Comparator.reverseOrder()).collect(
                Collectors.toList());
        ans.forEach(x->System.out.println(x));*/

        /*String s="abccba";
        Stack<Character> st=new Stack<>();
        for(int i=0;i<s.length();i++){
            char ch=s.charAt(i);
            if(!st.isEmpty()){
                if(st.peek()==ch){
                    st.pop();
                }
            }
            else{
                st.push(ch);
            }
        }
        boolean x=st.isEmpty()?true:false;
        System.out.println(x);*/
}
